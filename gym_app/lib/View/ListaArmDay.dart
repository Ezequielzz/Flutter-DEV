import 'package:flutter/material.dart';

class ListaArmDay extends StatelessWidget {
  const ListaArmDay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(),
    );
  }
}