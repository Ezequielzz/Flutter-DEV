import 'package:flutter/material.dart';

class ListaChestDay extends StatelessWidget {
  const ListaChestDay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exercícios de Peito'),
      ),
    );
  }
}